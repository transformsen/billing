import { CrmColumnIdentifierPipe } from './crm-column-identifier.pipe';

describe('CrmColumnIdentifierPipe', () => {
  it('create an instance', () => {
    const pipe = new CrmColumnIdentifierPipe();
    expect(pipe).toBeTruthy();
  });
});
