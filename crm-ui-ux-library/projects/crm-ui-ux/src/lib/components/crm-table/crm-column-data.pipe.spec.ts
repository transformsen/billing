import { CrmColumnDataPipe } from './crm-column-data.pipe';

describe('CrmColumnDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CrmColumnDataPipe();
    expect(pipe).toBeTruthy();
  });
});
