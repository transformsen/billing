import { CrmCustomCellDirective } from './crm-custom-cell.directive';

describe('CrmCustomCellDirective', () => {
  it('should create an instance', () => {
    const directive = new CrmCustomCellDirective();
    expect(directive).toBeTruthy();
  });
});
