import { CrmGroupItemDirective } from './crm-group-item.directive';

describe('CrmGroupItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CrmGroupItemDirective();
    expect(directive).toBeTruthy();
  });
});
